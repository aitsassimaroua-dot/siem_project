# DataSecurity-Project
A Mini SIEM using Flask + Elastic Stack for authentication log analysis

# Application de Login avec Journalisation

## Comment lancer l'application
1. Ouvrir un terminal dans le dossier `DATASECURITY-PROJECT/`.
2. Exécuter :
   ```bash
   cd app/
   python app.py
